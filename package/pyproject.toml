[build-system]
requires = [ "setuptools",]
build-backend = "setuptools.build_meta"

[project]
name = "jailmin"
version = "0.0.0+build.12"
dependencies = []

[project.scripts]
jailmin = "jailmin:doCli"
