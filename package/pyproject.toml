[build-system]
requires = [ "setuptools",]
build-backend = "setuptools.build_meta"

[project]
name = "jailmin"
version = "0.0.0+build.14"
dependencies = []

[project.scripts]
jailmin = "jailmin:doCli"
